# @ricozedu/web

Minimal Next.js UI for curriculum admin and student progress.

```bash
npx pnpm --filter @ricozedu/web dev
```

Requires API at `NEXT_PUBLIC_API_URL` (default `http://localhost:4000`).
