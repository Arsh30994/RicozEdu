export default function HomePage() {
  return (
    <section>
      <h1>Curriculum & rules</h1>
      <p className="muted">
        Versioned programmes, declarative rules, simulation before publish, and
        degree progress with human-readable explanations.
      </p>
      <div className="panel">
        <p>
          Use <a href="/login">Login</a> with your API access token and tenant
          id, then open Admin or Student views.
        </p>
      </div>
    </section>
  );
}
